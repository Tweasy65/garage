# Copy to .env.local for local dev.

# Clerk — same application as bartonhome.dev / lets-eat
VITE_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Neon Postgres — use the pooled `-pooler` host for Workers
DATABASE_URL=
